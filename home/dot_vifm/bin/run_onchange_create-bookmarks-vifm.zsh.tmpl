#!/usr/bin/env zsh
# Create ~/.vifm/bookmarks.vifm

target=${HOME}/.vifm/bookmarks.vifm

{{ template "bookmarks.zsh" }}

# File header
echo "\" Autogenerated by chezmoi" > ${target}

for name path in ${(kv)BOOKMARKS}; do
  echo "bmark! \"${path}\" ${name}"
done >> ${target}

exit 0
