" Configuration for MacVim when running as a GUI
" https://macvim-dev.github.io/macvim/

" Set font and size
set guifont=Monaco:h14

" Set default window size (about full screen)
set lines=60
set columns=162
