#
# Active Aliases
# I don't use the "unknown command helper" since that screws up the
# zsh-syntax-highlighting plugin.
#
# See http://gitolite.com/active-aliases/
# See ../../active_aliases.rc

# f <arg>
# Find any file with <arg> as part of name
alias f='__ f'
